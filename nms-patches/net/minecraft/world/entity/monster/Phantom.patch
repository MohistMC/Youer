--- a/net/minecraft/world/entity/monster/Phantom.java
+++ b/net/minecraft/world/entity/monster/Phantom.java
@@ -154,7 +154,7 @@
     @Override
     protected void readAdditionalSaveData(ValueInput valueinput) {
         super.readAdditionalSaveData(valueinput);
-        this.anchorPoint = (BlockPos) valueinput.read("anchor_pos", BlockPos.CODEC).orElse((Object) null);
+        this.anchorPoint = (BlockPos) valueinput.read("anchor_pos", BlockPos.CODEC).orElse(null); // CraftBukkit - decompile error
         this.setPhantomSize(valueinput.getIntOr("size", 0));
     }
 
@@ -524,11 +524,11 @@
                 List<Player> list = serverlevel.getNearbyPlayers(this.attackTargeting, Phantom.this, Phantom.this.getBoundingBox().inflate(16.0D, 64.0D, 16.0D));
 
                 if (!list.isEmpty()) {
-                    list.sort(Comparator.comparing(Entity::getY).reversed());
+                    list.sort(Comparator.comparing((Entity e) -> { return e.getY(); }).reversed()); // CraftBukkit - decompile error
 
                     for (Player player : list) {
                         if (Phantom.this.canAttack(serverlevel, player, TargetingConditions.DEFAULT)) {
-                            Phantom.this.setTarget(player);
+                            Phantom.this.setTarget(player, org.bukkit.event.entity.EntityTargetEvent.TargetReason.CLOSEST_PLAYER, true); // CraftBukkit - reason
                             return true;
                         }
                     }
