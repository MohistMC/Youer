--- a/net/minecraft/world/entity/animal/golem/IronGolem.java
+++ b/net/minecraft/world/entity/animal/golem/IronGolem.java
@@ -99,7 +99,7 @@
     @Override
     protected void doPush(Entity entity) {
         if (entity instanceof Enemy && !(entity instanceof Creeper) && this.getRandom().nextInt(20) == 0) {
-            this.setTarget((LivingEntity) entity);
+            this.setTarget((LivingEntity) entity, org.bukkit.event.entity.EntityTargetLivingEntityEvent.TargetReason.COLLISION, true); // CraftBukkit - set reason
         }
 
         super.doPush(entity);
@@ -129,7 +129,7 @@
 
     @Override
     public boolean canAttack(LivingEntity target) {
-        return this.isPlayerCreated() && target.is((Object) EntityType.PLAYER) ? false : (target.is((Object) EntityType.CREEPER) ? false : super.canAttack(target));
+        return this.isPlayerCreated() && target.is(EntityType.PLAYER) ? false : (target.is(EntityType.CREEPER) ? false : super.canAttack(target)); // CraftBukkit - decompile error
     }
 
     @Override
