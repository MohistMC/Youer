--- a/net/minecraft/world/entity/npc/villager/Villager.java
+++ b/net/minecraft/world/entity/npc/villager/Villager.java
@@ -94,6 +94,12 @@
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
+// CraftBukkit start
+import org.bukkit.Bukkit;
+import org.bukkit.event.entity.EntityTransformEvent;
+import org.bukkit.event.entity.VillagerReplenishTradeEvent;
+// CraftBukkit end
+
 public class Villager extends AbstractVillager implements VillagerDataHolder, ReputationEventHandler {
 
     private static final Logger LOGGER = LogUtils.getLogger();
@@ -149,15 +155,18 @@
         list.add(ActivityData.create(Activity.HIDE, VillagerGoalPackages.getHidePackage(0.5F)));
         return list;
     });
-    public static final Map<MemoryModuleType<GlobalPos>, BiPredicate<Villager, Holder<PoiType>>> POI_MEMORIES = ImmutableMap.of(MemoryModuleType.HOME, (BiPredicate) (villager, holder) -> {
+    public static final Map<MemoryModuleType<GlobalPos>, BiPredicate<Villager, Holder<PoiType>>> POI_MEMORIES = ImmutableMap.of(MemoryModuleType.HOME, (villager, holder) -> { // CraftBukkit - decompile error
         return holder.is(PoiTypes.HOME);
-    }, MemoryModuleType.JOB_SITE, (BiPredicate) (villager, holder) -> {
+    }, MemoryModuleType.JOB_SITE, (villager, holder) -> { // CraftBukkit - decompile error
         return ((VillagerProfession) villager.getVillagerData().profession().value()).heldJobSite().test(holder);
-    }, MemoryModuleType.POTENTIAL_JOB_SITE, (BiPredicate) (villager, holder) -> {
+    }, MemoryModuleType.POTENTIAL_JOB_SITE, (villager, holder) -> { // CraftBukkit - decompile error
         return VillagerProfession.ALL_ACQUIRABLE_JOBS.test(holder);
-    }, MemoryModuleType.MEETING_POINT, (BiPredicate) (villager, holder) -> {
+    }, MemoryModuleType.MEETING_POINT, (villager, holder) -> { // CraftBukkit - decompile error
         return holder.is(PoiTypes.MEETING);
     });
+    // CraftBukkit start
+    public long gossipDecayInterval = GOSSIP_DECAY_INTERVAL;
+    // CraftBukkit end
 
     public Villager(EntityType<? extends Villager> type, Level level) {
         this(type, level, VillagerType.PLAINS);
@@ -170,7 +179,9 @@
     public Villager(EntityType<? extends Villager> entityType, Level level, Holder<VillagerType> type) {
         super(entityType, level);
         this.foodLevel = 0;
-        this.gossips = new GossipContainer();
+        // CraftBukkit start - add constructor parameter in Reputation
+        this.gossips = new GossipContainer(this);
+        // CraftBukkit end
         this.lastGossipDecayTime = 0L;
         this.villagerXp = 0;
         this.lastRestockGameTime = 0L;
@@ -185,7 +196,7 @@
 
     @Override
     public Brain<Villager> getBrain() {
-        return super.getBrain();
+        return (Brain<Villager>) super.getBrain(); // CraftBukkit - decompile error
     }
 
     @Override
@@ -250,7 +261,7 @@
                     this.increaseProfessionLevelOnUpdate = false;
                 }
 
-                this.addEffect(new MobEffectInstance(MobEffects.REGENERATION, 200, 0));
+                this.addEffect(new MobEffectInstance(MobEffects.REGENERATION, 200, 0), org.bukkit.event.entity.EntityPotionEffectEvent.Cause.VILLAGER_TRADE); // CraftBukkit
             }
         }
 
@@ -373,7 +384,13 @@
         this.updateDemand();
 
         for (MerchantOffer merchantoffer : this.getOffers()) {
-            merchantoffer.resetUses();
+            // CraftBukkit start
+            VillagerReplenishTradeEvent event = new VillagerReplenishTradeEvent((org.bukkit.entity.Villager) this.getBukkitEntity(), merchantoffer.asBukkit());
+            Bukkit.getPluginManager().callEvent(event);
+            if (!event.isCancelled()) {
+                merchantoffer.resetUses();
+            }
+            // CraftBukkit end
         }
 
         this.resendOffersToTradingPlayer();
@@ -428,7 +445,13 @@
 
         if (i > 0) {
             for (MerchantOffer merchantoffer : this.getOffers()) {
-                merchantoffer.resetUses();
+                // CraftBukkit start
+                VillagerReplenishTradeEvent event = new VillagerReplenishTradeEvent((org.bukkit.entity.Villager) this.getBukkitEntity(), merchantoffer.asBukkit());
+                Bukkit.getPluginManager().callEvent(event);
+                if (!event.isCancelled()) {
+                    merchantoffer.resetUses();
+                }
+                // CraftBukkit end
             }
         }
 
@@ -511,7 +534,7 @@
 
         this.foodLevel = input.getByteOr("FoodLevel", (byte) 0);
         this.gossips.clear();
-        Optional optional1 = input.read("Gossips", GossipContainer.CODEC);
+        Optional<GossipContainer> optional1 = input.read("Gossips", GossipContainer.CODEC); // CraftBukkit - decompile error
         GossipContainer gossipcontainer = this.gossips;
 
         Objects.requireNonNull(this.gossips);
@@ -783,7 +806,7 @@
                 witch1.finalizeSpawn(level, level.getCurrentDifficultyAt(witch1.blockPosition()), EntitySpawnReason.CONVERSION, (SpawnGroupData) null);
                 witch1.setPersistenceRequired();
                 this.releaseAllPois();
-            });
+            }, EntityTransformEvent.TransformReason.LIGHTNING, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.LIGHTNING); // CraftBukkit
 
             if (witch == null) {
                 super.thunderHit(level, lightningBolt);
@@ -857,7 +880,7 @@
 
         if (this.lastGossipDecayTime == 0L) {
             this.lastGossipDecayTime = i;
-        } else if (i >= this.lastGossipDecayTime + 24000L) {
+        } else if (i >= this.lastGossipDecayTime + gossipDecayInterval) { // CraftBukkit - use variable for decay interval
             this.gossips.decay();
             this.lastGossipDecayTime = i;
         }
@@ -872,7 +895,7 @@
             }).limit(5L).toList();
 
             if (list1.size() >= villagersNeededToAgree) {
-                if (!SpawnUtil.trySpawnMob(EntityType.IRON_GOLEM, EntitySpawnReason.MOB_SUMMONED, level, this.blockPosition(), 10, 8, 6, SpawnUtil.Strategy.LEGACY_IRON_GOLEM, false).isEmpty()) {
+                if (!SpawnUtil.trySpawnMob(EntityType.IRON_GOLEM, EntitySpawnReason.MOB_SUMMONED, level, this.blockPosition(), 10, 8, 6, SpawnUtil.Strategy.LEGACY_IRON_GOLEM, false, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.VILLAGE_DEFENSE).isEmpty()) { // CraftBukkit
                     list.forEach(GolemSensor::golemDetected);
                 }
             }
@@ -885,15 +908,18 @@
 
     @Override
     public void onReputationEventFrom(ReputationEventType type, Entity source) {
+        org.bukkit.entity.Villager.ReputationEvent bukkitReputationEvent = org.bukkit.craftbukkit.entity.CraftVillager.CraftReputationEvent.minecraftToBukkit(type); // CraftBukkit - convert event to bukkit
         if (type == ReputationEventType.ZOMBIE_VILLAGER_CURED) {
-            this.gossips.add(source.getUUID(), GossipType.MAJOR_POSITIVE, 20);
-            this.gossips.add(source.getUUID(), GossipType.MINOR_POSITIVE, 25);
+            // CraftBukkit start - add change reason parameter
+            this.gossips.add(source.getUUID(), GossipType.MAJOR_POSITIVE, 20, bukkitReputationEvent);
+            this.gossips.add(source.getUUID(), GossipType.MINOR_POSITIVE, 25, bukkitReputationEvent);
+            // CraftBukkit end
         } else if (type == ReputationEventType.TRADE) {
-            this.gossips.add(source.getUUID(), GossipType.TRADING, 2);
+            this.gossips.add(source.getUUID(), GossipType.TRADING, 2, bukkitReputationEvent); // CraftBukkit - add change reason parameter
         } else if (type == ReputationEventType.VILLAGER_HURT) {
-            this.gossips.add(source.getUUID(), GossipType.MINOR_NEGATIVE, 25);
+            this.gossips.add(source.getUUID(), GossipType.MINOR_NEGATIVE, 25, bukkitReputationEvent); // CraftBukkit - add change reason parameter
         } else if (type == ReputationEventType.VILLAGER_KILLED) {
-            this.gossips.add(source.getUUID(), GossipType.MAJOR_NEGATIVE, 25);
+            this.gossips.add(source.getUUID(), GossipType.MAJOR_NEGATIVE, 25, bukkitReputationEvent); // CraftBukkit - add change reason parameter
         }
 
     }
