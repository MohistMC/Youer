--- a/net/minecraft/world/item/alchemy/PotionContents.java
+++ b/net/minecraft/world/item/alchemy/PotionContents.java
@@ -158,7 +158,7 @@
                 if (((MobEffect) mobeffectinstance.getEffect().value()).isInstantenous()) {
                     ((MobEffect) mobeffectinstance.getEffect().value()).applyInstantenousEffect(serverlevel, player2, player2, livingentity, mobeffectinstance.getAmplifier(), 1.0D);
                 } else {
-                    livingentity.addEffect(mobeffectinstance);
+                    livingentity.addEffect(mobeffectinstance, org.bukkit.event.entity.EntityPotionEffectEvent.Cause.POTION_DRINK); // CraftBukkit
                 }
 
             }, f);
