--- a/net/minecraft/world/item/enchantment/effects/ReplaceBlock.java
+++ b/net/minecraft/world/item/enchantment/effects/ReplaceBlock.java
@@ -26,7 +26,7 @@
 
         if ((Boolean) this.predicate.map((blockpredicate) -> {
             return blockpredicate.test(serverlevel, blockpos);
-        }).orElse(true) && serverlevel.setBlockAndUpdate(blockpos, this.blockState.getState(entity.getRandom(), blockpos))) {
+        }).orElse(true) && org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockFormEvent(serverlevel, blockpos, this.blockState.getState(entity.getRandom(), blockpos), entity)) { // CraftBukkit - Call EntityBlockFormEvent
             this.triggerGameEvent.ifPresent((holder) -> {
                 serverlevel.gameEvent(entity, holder, blockpos);
             });
