--- a/net/minecraft/world/level/block/CauldronBlock.java
+++ b/net/minecraft/world/level/block/CauldronBlock.java
@@ -13,6 +13,10 @@
 import net.minecraft.world.level.material.Fluid;
 import net.minecraft.world.level.material.Fluids;
 
+// CraftBukkit start
+import org.bukkit.event.block.CauldronLevelChangeEvent;
+// CraftBukkit end
+
 public class CauldronBlock extends AbstractCauldronBlock {
 
     public static final MapCodec<CauldronBlock> CODEC = simpleCodec(CauldronBlock::new);
@@ -61,14 +65,12 @@
         if (fluid == Fluids.WATER) {
             BlockState blockstate1 = Blocks.WATER_CAULDRON.defaultBlockState();
 
-            level.setBlockAndUpdate(blockpos, blockstate1);
-            level.gameEvent(GameEvent.BLOCK_CHANGE, blockpos, GameEvent.Context.of(blockstate1));
+            LayeredCauldronBlock.changeLevel(blockstate, level, blockpos, blockstate1, null, CauldronLevelChangeEvent.ChangeReason.NATURAL_FILL); // CraftBukkit
             level.levelEvent(1047, blockpos, 0);
         } else if (fluid == Fluids.LAVA) {
             BlockState blockstate2 = Blocks.LAVA_CAULDRON.defaultBlockState();
 
-            level.setBlockAndUpdate(blockpos, blockstate2);
-            level.gameEvent(GameEvent.BLOCK_CHANGE, blockpos, GameEvent.Context.of(blockstate2));
+            LayeredCauldronBlock.changeLevel(blockstate, level, blockpos, blockstate2, null, CauldronLevelChangeEvent.ChangeReason.NATURAL_FILL); // CraftBukkit
             level.levelEvent(1046, blockpos, 0);
         }
 
