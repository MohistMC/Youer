--- a/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/TheEndGatewayBlockEntity.java
@@ -27,6 +27,10 @@
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
+// CraftBukkit start
+import net.minecraft.world.level.dimension.LevelStem;
+// CraftBukkit end
+
 public class TheEndGatewayBlockEntity extends TheEndPortalBlockEntity {
 
     private static final Logger LOGGER = LogUtils.getLogger();
@@ -61,7 +65,7 @@
     protected void loadAdditional(ValueInput valueinput) {
         super.loadAdditional(valueinput);
         this.age = valueinput.getLongOr("Age", 0L);
-        this.exitPortal = (BlockPos) valueinput.read("exit_portal", BlockPos.CODEC).filter(Level::isInSpawnableBounds).orElse((Object) null);
+        this.exitPortal = (BlockPos) valueinput.read("exit_portal", BlockPos.CODEC).filter(Level::isInSpawnableBounds).orElse(null); // CraftBukkit - decompile error
         this.exactTeleport = valueinput.getBooleanOr("ExactTeleport", false);
     }
 
@@ -136,7 +140,7 @@
     }
 
     public @Nullable Vec3 getPortalPosition(ServerLevel serverlevel, BlockPos blockpos) {
-        if (this.exitPortal == null && serverlevel.dimension() == Level.END) {
+        if (this.exitPortal == null && serverlevel.getTypeKey() == LevelStem.END) { // CraftBukkit - work in alternate worlds
             BlockPos blockpos1 = findOrCreateValidTeleportPos(serverlevel, blockpos);
 
             blockpos1 = blockpos1.above(10);
