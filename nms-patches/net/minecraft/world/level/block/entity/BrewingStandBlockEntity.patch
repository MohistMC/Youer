--- a/net/minecraft/world/level/block/entity/BrewingStandBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/BrewingStandBlockEntity.java
@@ -24,6 +24,20 @@
 import net.minecraft.world.level.storage.ValueOutput;
 import org.jspecify.annotations.Nullable;
 
+// CraftBukkit start
+import java.util.ArrayList;
+import java.util.List;
+import net.minecraft.server.MinecraftServer;
+import org.bukkit.craftbukkit.block.CraftBlock;
+import org.bukkit.craftbukkit.entity.CraftHumanEntity;
+import org.bukkit.craftbukkit.inventory.CraftItemStack;
+import org.bukkit.entity.HumanEntity;
+import org.bukkit.event.block.BrewingStartEvent;
+import org.bukkit.event.inventory.BrewEvent;
+import org.bukkit.event.inventory.BrewingStandFuelEvent;
+import org.bukkit.inventory.InventoryHolder;
+// CraftBukkit end
+
 public class BrewingStandBlockEntity extends BaseContainerBlockEntity implements WorldlyContainer {
 
     private static final int INGREDIENT_SLOT = 3;
@@ -44,6 +58,36 @@
     private Item ingredient;
     public int fuel;
     protected final ContainerData dataAccess;
+    // CraftBukkit start - add fields and methods
+    private int lastTick = MinecraftServer.currentTick;
+    public List<HumanEntity> transaction = new java.util.ArrayList<HumanEntity>();
+    private int maxStack = MAX_STACK;
+
+    public void onOpen(CraftHumanEntity who) {
+        transaction.add(who);
+    }
+
+    public void onClose(CraftHumanEntity who) {
+        transaction.remove(who);
+    }
+
+    public List<HumanEntity> getViewers() {
+        return transaction;
+    }
+
+    public List<ItemStack> getContents() {
+        return this.items;
+    }
+
+    @Override
+    public int getMaxStackSize() {
+        return maxStack;
+    }
+
+    public void setMaxStackSize(int size) {
+        maxStack = size;
+    }
+    // CraftBukkit end
 
     public BrewingStandBlockEntity(BlockPos blockpos, BlockState blockstate) {
         super(BlockEntityType.BREWING_STAND, blockpos, blockstate);
@@ -110,8 +154,19 @@
         ItemStack itemstack = brewingstandblockentity.items.get(4);
 
         if (brewingstandblockentity.fuel <= 0 && itemstack.is(ItemTags.BREWING_FUEL)) {
-            brewingstandblockentity.fuel = 20;
-            itemstack.shrink(1);
+            // CraftBukkit start
+            BrewingStandFuelEvent event = new BrewingStandFuelEvent(CraftBlock.at(level, blockpos), CraftItemStack.asCraftMirror(itemstack), 20);
+            level.getCraftServer().getPluginManager().callEvent(event);
+
+            if (event.isCancelled()) {
+                return;
+            }
+
+            brewingstandblockentity.fuel = event.getFuelPower();
+            if (brewingstandblockentity.fuel > 0 && event.isConsuming()) {
+                itemstack.shrink(1);
+            }
+            // CraftBukkit end
             setChanged(level, blockpos, blockstate);
         }
 
@@ -119,12 +174,17 @@
         boolean flag1 = brewingstandblockentity.brewTime > 0;
         ItemStack itemstack1 = brewingstandblockentity.items.get(3);
 
+        // CraftBukkit start - Use wall time instead of ticks for brewing
+        int elapsedTicks = MinecraftServer.currentTick - brewingstandblockentity.lastTick;
+        brewingstandblockentity.lastTick = MinecraftServer.currentTick;
+
         if (flag1) {
-            --brewingstandblockentity.brewTime;
-            boolean flag2 = brewingstandblockentity.brewTime == 0;
+            brewingstandblockentity.brewTime -= elapsedTicks;
+            boolean flag2 = brewingstandblockentity.brewTime <= 0; // == -> <=
+            // CraftBukkit end
 
             if (flag2 && flag) {
-                doBrew(level, blockpos, brewingstandblockentity.items);
+                doBrew(level, blockpos, brewingstandblockentity.items, brewingstandblockentity); // CraftBukkit
             } else if (!flag || !itemstack1.is(brewingstandblockentity.ingredient)) {
                 brewingstandblockentity.brewTime = 0;
             }
@@ -132,7 +192,11 @@
             setChanged(level, blockpos, blockstate);
         } else if (flag && brewingstandblockentity.fuel > 0) {
             --brewingstandblockentity.fuel;
-            brewingstandblockentity.brewTime = 400;
+            // CraftBukkit start
+            BrewingStartEvent event = new BrewingStartEvent(CraftBlock.at(level, blockpos), CraftItemStack.asCraftMirror(itemstack1), 400);
+            level.getCraftServer().getPluginManager().callEvent(event);
+            brewingstandblockentity.brewTime = event.getTotalBrewTime(); // 400 -> event.getTotalBrewTime()
+            // CraftBukkit end
             brewingstandblockentity.ingredient = itemstack1.getItem();
             setChanged(level, blockpos, blockstate);
         }
@@ -188,12 +252,34 @@
         }
     }
 
-    private static void doBrew(Level level, BlockPos blockpos, NonNullList<ItemStack> nonnulllist) {
+    private static void doBrew(Level level, BlockPos blockpos, NonNullList<ItemStack> nonnulllist, BrewingStandBlockEntity brewingstandblockentity) { // CraftBukkit
         ItemStack itemstack = nonnulllist.get(3);
         PotionBrewing potionbrewing = level.potionBrewing();
 
+        // CraftBukkit start
+        InventoryHolder owner = brewingstandblockentity.getOwner();
+        List<org.bukkit.inventory.ItemStack> brewResults = new ArrayList<>(3);
         for (int i = 0; i < 3; ++i) {
-            nonnulllist.set(i, potionbrewing.mix(itemstack, nonnulllist.get(i)));
+            brewResults.add(i, CraftItemStack.asCraftMirror(potionbrewing.mix(itemstack, nonnulllist.get(i))));
+        }
+
+        if (owner != null) {
+            BrewEvent event = new BrewEvent(CraftBlock.at(level, blockpos), (org.bukkit.inventory.BrewerInventory) owner.getInventory(), brewResults, brewingstandblockentity.fuel);
+            org.bukkit.Bukkit.getPluginManager().callEvent(event);
+            if (event.isCancelled()) {
+                return;
+            }
+        }
+        // CraftBukkit end
+
+        for (int i = 0; i < 3; ++i) {
+            // CraftBukkit start - validate index in case it is cleared by plugins
+            if (i < brewResults.size()) {
+                nonnulllist.set(i, CraftItemStack.asNMSCopy(brewResults.get(i)));
+            } else {
+                nonnulllist.set(i, ItemStack.EMPTY);
+            }
+            // CraftBukkit end
         }
 
         itemstack.shrink(1);
