--- a/net/minecraft/server/level/PlayerSpawnFinder.java
+++ b/net/minecraft/server/level/PlayerSpawnFinder.java
@@ -52,7 +52,7 @@
     }
 
     public static CompletableFuture<Vec3> findSpawn(ServerLevel serverlevel, BlockPos blockpos) {
-        if (serverlevel.dimensionType().hasSkyLight() && serverlevel.getServer().getWorldData().getGameType() != GameType.ADVENTURE) {
+        if (serverlevel.dimensionType().hasSkyLight() && serverlevel.serverLevelData.getGameType() != GameType.ADVENTURE) { // CraftBukkit
             int i = Math.max(0, (Integer) serverlevel.getGameRules().get(GameRules.RESPAWN_RADIUS));
             int j = Mth.floor(serverlevel.getWorldBorder().getDistanceToBorder((double) blockpos.getX(), (double) blockpos.getZ()));
 
