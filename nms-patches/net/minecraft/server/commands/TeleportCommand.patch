--- a/net/minecraft/server/commands/TeleportCommand.java
+++ b/net/minecraft/server/commands/TeleportCommand.java
@@ -31,6 +31,14 @@
 import net.minecraft.world.phys.Vec3;
 import org.jspecify.annotations.Nullable;
 
+// CraftBukkit start
+import net.minecraft.server.level.ServerPlayer;
+import org.bukkit.Location;
+import org.bukkit.craftbukkit.CraftWorld;
+import org.bukkit.event.entity.EntityTeleportEvent;
+import org.bukkit.event.player.PlayerTeleportEvent;
+// CraftBukkit end
+
 public class TeleportCommand {
 
     private static final SimpleCommandExceptionType INVALID_POSITION = new SimpleCommandExceptionType(Component.translatable("commands.teleport.invalidPosition"));
@@ -130,7 +138,30 @@
             float f4 = Mth.wrapDegrees(f2);
             float f5 = Mth.wrapDegrees(f3);
 
-            if (entity.teleportTo(serverlevel, d3, d4, d5, set, f4, f5, true)) {
+            // CraftBukkit start - Teleport event
+            boolean result;
+            if (entity instanceof ServerPlayer player) {
+                result = player.teleportTo(serverlevel, d3, d4, d5, set, f4, f5, true, PlayerTeleportEvent.TeleportCause.COMMAND);
+            } else {
+                Location to = new Location(serverlevel.getWorld(), d3, d4, d5, f4, f5);
+                EntityTeleportEvent event = new EntityTeleportEvent(entity.getBukkitEntity(), entity.getBukkitEntity().getLocation(), to);
+                serverlevel.getCraftServer().getPluginManager().callEvent(event);
+                if (event.isCancelled()) {
+                    return;
+                }
+
+                d3 = to.getX();
+                d4 = to.getY();
+                d5 = to.getZ();
+                f4 = to.getYaw();
+                f5 = to.getPitch();
+                serverlevel = ((CraftWorld) to.getWorld()).getHandle();
+
+                result = entity.teleportTo(serverlevel, d3, d4, d5, set, f4, f5, true);
+            }
+
+            if (result) {
+                // CraftBukkit end
                 if (lookat != null) {
                     lookat.perform(commandsourcestack, entity);
                 }
