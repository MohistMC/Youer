--- a/net/minecraft/world/entity/projectile/DragonFireball.java
+++ b/net/minecraft/world/entity/projectile/DragonFireball.java
@@ -54,8 +_,9 @@
                 }
 
                 this.level().levelEvent(2006, this.blockPosition(), this.isSilent() ? -1 : 1);
+                areaeffectcloud.spawnReason(org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.EXPLOSION); // Paper - use correct spawn reason
                 this.level().addFreshEntity(areaeffectcloud);
-                this.discard();
+                this.discard(org.bukkit.event.entity.EntityRemoveEvent.Cause.HIT); // CraftBukkit - add Bukkit remove cause
             }
         }
     }
