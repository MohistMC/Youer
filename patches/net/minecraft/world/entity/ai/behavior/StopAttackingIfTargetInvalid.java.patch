--- a/net/minecraft/world/entity/ai/behavior/StopAttackingIfTargetInvalid.java
+++ b/net/minecraft/world/entity/ai/behavior/StopAttackingIfTargetInvalid.java
@@ -6,6 +_,9 @@
 import net.minecraft.world.entity.Mob;
 import net.minecraft.world.entity.ai.behavior.declarative.BehaviorBuilder;
 import net.minecraft.world.entity.ai.memory.MemoryModuleType;
+import org.bukkit.craftbukkit.entity.CraftLivingEntity;
+import org.bukkit.craftbukkit.event.CraftEventFactory;
+import org.bukkit.event.entity.EntityTargetEvent;
 
 public class StopAttackingIfTargetInvalid {
     private static final int TIMEOUT_TO_GET_WITHIN_ATTACK_RANGE = 200;
@@ -39,11 +_,35 @@
                             && target.level() == body.level()
                             && !stopAttackingWhen.test(level, target)) {
                             return true;
+                        } else {
+                            // Paper start - better track target change reason
+                            final EntityTargetEvent.TargetReason reason;
+                            if (!body.canAttack(target)) {
+                                reason = EntityTargetEvent.TargetReason.TARGET_INVALID;
+                            } else if (canGrowTiredOfTryingToReachTarget && StopAttackingIfTargetInvalid.isTiredOfTryingToReachTarget(body, i.tryGet(cantReachSince))) {
+                                reason = EntityTargetEvent.TargetReason.FORGOT_TARGET;
+                            } else if (!target.isAlive()) {
+                                reason = EntityTargetEvent.TargetReason.TARGET_DIED;
+                            } else if (target.level() != body.level()) {
+                                reason = EntityTargetEvent.TargetReason.TARGET_OTHER_LEVEL;
+                            } else {
+                                reason = EntityTargetEvent.TargetReason.TARGET_INVALID;
+                            }
+                            // Paper end
+                            // CraftBukkit start
+                            EntityTargetEvent event = CraftEventFactory.callEntityTargetLivingEvent(body, null, reason); // Paper
+                            if (event.isCancelled()) {
+                                return false;
+                            }
+                            if (event.getTarget() != null) {
+                                body.getBrain().setMemory(MemoryModuleType.ATTACK_TARGET, ((CraftLivingEntity) event.getTarget()).getHandle());
+                                return true;
+                            }
+                            // CraftBukkit end
+                            onTargetErased.accept(level, body, target);
+                            attackTarget.erase();
+                            return true;
                         }
-
-                        onTargetErased.accept(level, body, target);
-                        attackTarget.erase();
-                        return true;
                     }
                 )
         );
