--- a/net/minecraft/world/entity/ai/goal/RunAroundLikeCrazyGoal.java
+++ b/net/minecraft/world/entity/ai/goal/RunAroundLikeCrazyGoal.java
@@ -6,6 +_,8 @@
 import net.minecraft.world.entity.animal.horse.AbstractHorse;
 import net.minecraft.world.entity.player.Player;
 import net.minecraft.world.phys.Vec3;
+import org.bukkit.craftbukkit.entity.CraftHumanEntity;
+import org.bukkit.craftbukkit.event.CraftEventFactory;
 
 public class RunAroundLikeCrazyGoal extends Goal {
     private final AbstractHorse horse;
@@ -58,7 +_,7 @@
             if (entity instanceof Player player) {
                 int i = this.horse.getTemper();
                 int j = this.horse.getMaxTemper();
-                if (j > 0 && this.horse.getRandom().nextInt(j) < i) {
+                if (j > 0 && this.horse.getRandom().nextInt(j) < i && !net.neoforged.neoforge.event.EventHooks.onAnimalTame(horse, (Player)entity) && !CraftEventFactory.callEntityTameEvent(this.horse, ((CraftHumanEntity) this.horse.getBukkitEntity().getPassenger()).getHandle()).isCancelled()) { // CraftBukkit - fire EntityTameEvent
                     this.horse.tameWithName(player);
                     return;
                 }
