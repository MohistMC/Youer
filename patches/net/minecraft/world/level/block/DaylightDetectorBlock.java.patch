--- a/net/minecraft/world/level/block/DaylightDetectorBlock.java
+++ b/net/minecraft/world/level/block/DaylightDetectorBlock.java
@@ -58,7 +_,8 @@
 
         target = Mth.clamp(target, 0, 15);
         if (state.getValue(POWER) != target) {
-            level.setBlockAndUpdate(pos, state.setValue(POWER, target));
+            target = org.bukkit.craftbukkit.event.CraftEventFactory.callRedstoneChange(level, pos, state.getValue(POWER), target).getNewCurrent(); // CraftBukkit - Call BlockRedstoneEvent
+            level.setBlock(pos, state.setValue(POWER, target), 3);
         }
     }
 
