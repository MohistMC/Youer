--- a/net/minecraft/world/level/storage/LevelStorageSource.java
+++ b/net/minecraft/world/level/storage/LevelStorageSource.java
@@ -62,6 +_,7 @@
 import net.minecraft.util.datafix.DataFixers;
 import net.minecraft.world.flag.FeatureFlagSet;
 import net.minecraft.world.flag.FeatureFlags;
+import net.minecraft.world.level.GameRules;
 import net.minecraft.world.level.Level;
 import net.minecraft.world.level.LevelSettings;
 import net.minecraft.world.level.WorldDataConfiguration;
@@ -144,6 +_,8 @@
         PrimaryLevelData primaryleveldata = PrimaryLevelData.parse(
             dynamic, levelsettings, worlddimensions$complete.specialWorldProperty(), worldgensettings.options(), lifecycle
         );
+        primaryleveldata.pdc = ((Dynamic<Tag>) dynamic).getElement("BukkitValues", null); // CraftBukkit - Add PDC to world
+
         return new LevelDataAndDimensions(primaryleveldata, worlddimensions$complete);
     }
 
@@ -343,7 +_,7 @@
 
     public LevelStorageSource.LevelStorageAccess validateAndCreateAccess(String p_289980_) throws IOException, ContentValidationException {
         Path path = this.getLevelPath(p_289980_);
-        List<ForbiddenSymlinkInfo> list = this.worldDirValidator.validateDirectory(path, true);
+        List<ForbiddenSymlinkInfo> list = Boolean.getBoolean("paper.disableWorldSymlinkValidation") ? List.of() : this.worldDirValidator.validateDirectory(path, true);
         if (!list.isEmpty()) {
             throw new ContentValidationException(path, list);
         } else {
@@ -351,15 +_,44 @@
         }
     }
 
+    public LevelStorageSource.LevelStorageAccess validateAndCreateAccess(String p_289980_, ResourceKey<LevelStem> dimensionType) throws IOException, ContentValidationException {
+        Path path = this.getLevelPath(p_289980_);
+        List<ForbiddenSymlinkInfo> list = Boolean.getBoolean("paper.disableWorldSymlinkValidation") ? List.of() : this.worldDirValidator.validateDirectory(path, true);
+        if (!list.isEmpty()) {
+            throw new ContentValidationException(path, list);
+        } else {
+            return new LevelStorageSource.LevelStorageAccess(p_289980_, path, dimensionType); // CraftBukkit
+        }
+    }
+
     public LevelStorageSource.LevelStorageAccess createAccess(String p_78261_) throws IOException {
         Path path = this.getLevelPath(p_78261_);
         return new LevelStorageSource.LevelStorageAccess(p_78261_, path);
     }
 
+    public LevelStorageSource.LevelStorageAccess createAccess(String p_78261_, ResourceKey<LevelStem> dimensionType) throws IOException {
+        Path path = this.getLevelPath(p_78261_);
+        return new LevelStorageSource.LevelStorageAccess(p_78261_, path, dimensionType);
+    }
+
     public DirectoryValidator getWorldDirValidator() {
         return this.worldDirValidator;
     }
 
+    // CraftBukkit start
+    public static Path getStorageFolder(Path path, ResourceKey<LevelStem> dimensionType) {
+        if (dimensionType == LevelStem.OVERWORLD) {
+            return path;
+        } else if (dimensionType == LevelStem.NETHER) {
+            return path.resolve("DIM-1");
+        } else if (dimensionType == LevelStem.END) {
+            return path.resolve("DIM1");
+        } else {
+            return path.resolve("dimensions").resolve(dimensionType.location().getNamespace()).resolve(dimensionType.location().getPath());
+        }
+    }
+    // CraftBukkit end
+
     public static record LevelCandidates(List<LevelStorageSource.LevelDirectory> levels) implements Iterable<LevelStorageSource.LevelDirectory> {
         public boolean isEmpty() {
             return this.levels.isEmpty();
@@ -410,8 +_,19 @@
         public final LevelStorageSource.LevelDirectory levelDirectory;
         private final String levelId;
         private final Map<LevelResource, Path> resources = Maps.newHashMap();
-
-        LevelStorageAccess(String p_289967_, Path p_289988_) throws IOException {
+        // CraftBukkit start
+        public ResourceKey<LevelStem> dimensionType;
+
+        public LevelStorageAccess(String p_289967_, Path p_289988_) throws IOException {
+            this.levelId = p_289967_;
+            this.levelDirectory = new LevelStorageSource.LevelDirectory(p_289988_);
+            this.lock = DirectoryLock.create(p_289988_);
+            this.dimensionType = null;
+        }
+
+        public LevelStorageAccess(String p_289967_, Path p_289988_, final ResourceKey<LevelStem> dimensionType) throws IOException {
+            this.dimensionType = dimensionType;
+            // CraftBukkit end
             this.levelId = p_289967_;
             this.levelDirectory = new LevelStorageSource.LevelDirectory(p_289988_);
             this.lock = DirectoryLock.create(p_289988_);
@@ -454,7 +_,7 @@
         }
 
         public Path getDimensionPath(ResourceKey<Level> p_197395_) {
-            return DimensionType.getStorageFolder(p_197395_, this.levelDirectory.path());
+            return dimensionType != null ? LevelStorageSource.getStorageFolder(this.levelDirectory.path(), this.dimensionType) : DimensionType.getStorageFolder(p_197395_, this.levelDirectory.path());
         }
 
         private void checkLock() {
@@ -463,6 +_,19 @@
             }
         }
 
+        // Neo: For reading NeoForge's additional save data (mod list, etc.)
+        public void readAdditionalLevelSaveData(boolean fallback) {
+            checkLock();
+            Path path = fallback ? this.levelDirectory.oldDataFile() : this.levelDirectory.dataFile();
+            try {
+                var tag = readLightweightData(path);
+                if (tag instanceof CompoundTag compoundTag)
+                    net.neoforged.neoforge.common.CommonHooks.readAdditionalLevelSaveData(compoundTag, this.levelDirectory);
+            } catch (Exception e) {
+                LOGGER.error("Exception reading {}", path, e);
+            }
+        }
+
         public PlayerDataStorage createPlayerStorage() {
             this.checkLock();
             return new PlayerDataStorage(this, LevelStorageSource.this.fixerUpper);
@@ -496,27 +_,81 @@
             CompoundTag compoundtag = p_78292_.createTag(p_78291_, p_78293_);
             CompoundTag compoundtag1 = new CompoundTag();
             compoundtag1.put("Data", compoundtag);
+            net.neoforged.neoforge.common.CommonHooks.writeAdditionalLevelSaveData(p_78292_, compoundtag1);
+            // Youer - writes are async; the main thread only waits for them on shutdown (see close())
             this.saveLevelData(compoundtag1);
         }
 
+        // Youer start - per-dimension level.dat, written off the server thread
+        // pendingLevelDataWrites is only touched on the server thread (submit in
+        // ServerLevel.save / saveDataTag, await in saveLevelData); each tag is
+        // read-only once submitted, so the background thread never races the main thread.
+        private final List<CompletableFuture<Void>> pendingLevelDataWrites = new ArrayList<>();
+
+        // Saves the dimension's own world data into its folder (DIM-1, DIM1, dimensions/...), like Bukkit
+        // worlds each have their own level.dat, instead of overwriting the main world/level.dat.
+        public void saveDimensionDataTag(RegistryAccess p_78288_, WorldData p_78289_, @Nullable CompoundTag p_78293_, ResourceKey<Level> dimension, GameRules dimensionGameRules) {
+            CompoundTag compoundtag = p_78289_.createTag(p_78288_, p_78293_);
+            CompoundTag compoundtag1 = new CompoundTag();
+            compoundtag1.put("Data", compoundtag);
+            net.neoforged.neoforge.common.CommonHooks.writeAdditionalLevelSaveData(p_78289_, compoundtag1);
+            // the shared settings' GameRules are the main world's; store the dimension's own rules instead
+            compoundtag.put("GameRules", dimensionGameRules.createTag());
+            // use getDimensionPath which resolves through this access's dimensionType
+            // Youer - writes are async; the main thread only waits for them on shutdown (see close())
+            this.submitLevelDataWrite(compoundtag1, this.getDimensionPath(dimension));
+        }
+
         private void saveLevelData(CompoundTag p_307262_) {
-            Path path = this.levelDirectory.path();
-
-            try {
-                Path path1 = Files.createTempFile(path, "level", ".dat");
-                NbtIo.writeCompressed(p_307262_, path1);
-                Path path2 = this.levelDirectory.oldDataFile();
-                Path path3 = this.levelDirectory.dataFile();
-                Util.safeReplaceFile(path3, path1, path2);
-            } catch (Exception exception) {
-                LevelStorageSource.LOGGER.error("Failed to save level {}", path, exception);
-            }
-        }
+            // Youer - writes are async; the main thread only waits for them on shutdown (see close())
+            this.submitLevelDataWrite(p_307262_, this.levelDirectory.path());
+        }
+
+        // NBT encoding + GZIP + disk write + atomic rename all run on the background
+        // executor, so a completed future means level.dat is fully written.
+        private void submitLevelDataWrite(CompoundTag p_307262_, Path directory) {
+            try {
+                // The main thread no longer awaits each write during auto-save, so drop
+                // completed futures here to keep the pending list bounded until shutdown.
+                this.pendingLevelDataWrites.removeIf(CompletableFuture::isDone);
+                Files.createDirectories(directory);
+                Path path1 = Files.createTempFile(directory, "level", ".dat");
+                Path path2 = directory.resolve("level.dat_old");
+                Path path3 = directory.resolve("level.dat");
+                CompletableFuture<Void> future = CompletableFuture.runAsync(() -> {
+                    try {
+                        NbtIo.writeCompressed(p_307262_, path1);
+                        Util.safeReplaceFile(path3, path1, path2);
+                    } catch (IOException e) {
+                        throw new UncheckedIOException(e);
+                    }
+                }, Util.backgroundExecutor());
+                this.pendingLevelDataWrites.add(future);
+            } catch (Exception exception) {
+                LevelStorageSource.LOGGER.error("Failed to save level {}", directory, exception);
+            }
+        }
+
+        public void awaitPendingLevelDataWrites() {
+            if (this.pendingLevelDataWrites.isEmpty()) return;
+            List<CompletableFuture<Void>> futures = new ArrayList<>(this.pendingLevelDataWrites);
+            this.pendingLevelDataWrites.clear();
+            try {
+                CompletableFuture.allOf(futures.toArray(CompletableFuture[]::new)).join();
+            } catch (Exception exception) {
+                LevelStorageSource.LOGGER.error("Failed to await level data writes", exception);
+            }
+        }
+        // Youer end
 
         public Optional<Path> getIconFile() {
             return !this.lock.isValid() ? Optional.empty() : Optional.of(this.levelDirectory.iconFile());
         }
 
+        public Path getWorldDir() {
+            return baseDir;
+        }
+
         public void deleteLevel() throws IOException {
             this.checkLock();
             final Path path = this.levelDirectory.lockFile();
@@ -624,6 +_,8 @@
 
         @Override
         public void close() throws IOException {
+            // Youer - guarantee level.dat writes are flushed to disk on shutdown
+            this.awaitPendingLevelDataWrites();
             this.lock.close();
         }
 
