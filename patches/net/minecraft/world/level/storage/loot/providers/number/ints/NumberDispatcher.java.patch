--- a/net/minecraft/world/level/storage/loot/providers/number/ints/NumberDispatcher.java
+++ /dev/null
@@ -1,25 +_,0 @@
-package net.minecraft.world.level.storage.loot.providers.number.ints;
-
-import com.mojang.serialization.MapCodec;
-import java.util.List;
-import net.minecraft.core.Holder;
-import net.minecraft.world.level.storage.loot.LootContext;
-import net.minecraft.world.level.storage.loot.providers.number.DispatcherProvider;
-
-public record NumberDispatcher(List<DispatcherProvider.Case<ContextIntProvider>> cases, Holder<ContextIntProvider> defaultValue)
-    implements ContextIntProvider,
-    DispatcherProvider<ContextIntProvider> {
-    public static final MapCodec<NumberDispatcher> MAP_CODEC = DispatcherProvider.mapCodec(
-        ContextIntProviders.CODEC, ContextIntProviders.exactly(0), NumberDispatcher::new
-    );
-
-    @Override
-    public int getIntUnsafe(LootContext context) {
-        return this.selectValue(context).value().getIntUnsafe(context);
-    }
-
-    @Override
-    public MapCodec<NumberDispatcher> codec() {
-        return MAP_CODEC;
-    }
-}
