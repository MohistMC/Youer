--- a/net/minecraft/world/level/storage/loot/providers/number/ints/Difference.java
+++ /dev/null
@@ -1,20 +_,0 @@
-package net.minecraft.world.level.storage.loot.providers.number.ints;
-
-import com.mojang.serialization.MapCodec;
-import net.minecraft.core.Holder;
-import net.minecraft.world.level.storage.loot.LootContext;
-import net.minecraft.world.level.storage.loot.providers.number.BinaryProvider;
-
-public record Difference(Holder<ContextIntProvider> left, Holder<ContextIntProvider> right) implements ContextIntProvider, BinaryProvider<ContextIntProvider> {
-    public static final MapCodec<Difference> MAP_CODEC = BinaryProvider.mapCodec(ContextIntProviders.CODEC, Difference::new);
-
-    @Override
-    public MapCodec<Difference> codec() {
-        return MAP_CODEC;
-    }
-
-    @Override
-    public int getIntUnsafe(LootContext context) {
-        return Math.subtractExact(this.left().value().getIntUnsafe(context), this.right().value().getIntUnsafe(context));
-    }
-}
