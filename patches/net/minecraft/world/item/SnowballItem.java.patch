--- a/net/minecraft/world/item/SnowballItem.java
+++ b/net/minecraft/world/item/SnowballItem.java
@@ -20,25 +_,31 @@
     @Override
     public InteractionResultHolder<ItemStack> use(Level p_43142_, Player p_43143_, InteractionHand p_43144_) {
         ItemStack itemstack = p_43143_.getItemInHand(p_43144_);
-        p_43142_.playSound(
-            null,
-            p_43143_.getX(),
-            p_43143_.getY(),
-            p_43143_.getZ(),
-            SoundEvents.SNOWBALL_THROW,
-            SoundSource.NEUTRAL,
-            0.5F,
-            0.4F / (p_43142_.getRandom().nextFloat() * 0.4F + 0.8F)
-        );
         if (!p_43142_.isClientSide) {
             Snowball snowball = new Snowball(p_43142_, p_43143_);
             snowball.setItem(itemstack);
             snowball.shootFromRotation(p_43143_, p_43143_.getXRot(), p_43143_.getYRot(), 0.0F, 1.5F, 1.0F);
-            p_43142_.addFreshEntity(snowball);
+            // Paper start - PlayerLaunchProjectileEvent
+            com.destroystokyo.paper.event.player.PlayerLaunchProjectileEvent event = new com.destroystokyo.paper.event.player.PlayerLaunchProjectileEvent((org.bukkit.entity.Player) p_43143_.getBukkitEntity(), org.bukkit.craftbukkit.inventory.CraftItemStack.asCraftMirror(itemstack), (org.bukkit.entity.Projectile) snowball.getBukkitEntity());
+            if (event.callEvent() && p_43142_.addFreshEntity(snowball)) {
+                p_43143_.awardStat(Stats.ITEM_USED.get(this));
+                if (event.shouldConsume()) {
+                    // Paper end - PlayerLaunchProjectileEvent
+                    itemstack.consume(1, p_43143_);
+                } else if (p_43143_ instanceof net.minecraft.server.level.ServerPlayer) {  // Paper - PlayerLaunchProjectileEvent
+                    ((net.minecraft.server.level.ServerPlayer) p_43143_).getBukkitEntity().updateInventory();  // Paper - PlayerLaunchProjectileEvent
+                }
+
+                p_43142_.playSound((Player) null, p_43143_.getX(), p_43143_.getY(), p_43143_.getZ(), SoundEvents.SNOWBALL_THROW, SoundSource.NEUTRAL, 0.5F, 0.4F / (p_43142_.getRandom().nextFloat() * 0.4F + 0.8F));
+            } else { // Paper - PlayerLaunchProjectileEvent
+                if (p_43143_ instanceof net.minecraft.server.level.ServerPlayer) ((net.minecraft.server.level.ServerPlayer) p_43143_).getBukkitEntity().updateInventory(); // Paper - PlayerLaunchProjectileEvent
+                return InteractionResultHolder.fail(itemstack); // Paper - PlayerLaunchProjectileEvent
+            }
         }
+        // CraftBukkit end
 
-        p_43143_.awardStat(Stats.ITEM_USED.get(this));
-        itemstack.consume(1, p_43143_);
+        // p_43143_.awardStat(Stats.ITEM_USED.get(this)); // Paper - PlayerLaunchProjectileEvent; moved up
+        // itemstack.consume(1, p_43143_); // CraftBukkit - moved up
         return InteractionResultHolder.sidedSuccess(itemstack, p_43142_.isClientSide());
     }
 
