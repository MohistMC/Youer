--- a/net/minecraft/client/gui/screens/worldselection/WorldOpenFlows.java
+++ b/net/minecraft/client/gui/screens/worldselection/WorldOpenFlows.java
@@ -218,7 +_,10 @@
             backupWarning = Component.translatable("selectWorld.backupWarning.customized");
         } else {
             backupQuestion = Component.translatable("selectWorld.backupQuestion.experimental");
-            backupWarning = Component.translatable("selectWorld.backupWarning.experimental");
+            // Neo: Add a line saying that the message won't show again.
+            backupWarning = Component.translatable("selectWorld.backupWarning.experimental")
+                    .append("\n\n")
+                    .append(Component.translatable("neoforge.selectWorld.backupWarning.experimental.additional"));
         }
 
         this.minecraft.setScreen(new BackupConfirmScreen(cancelCallback, (backup, eraseCache) -> {
@@ -273,6 +_,7 @@
         try {
             levelDataTag = worldAccess.getDataTag();
             summary = worldAccess.getSummary(levelDataTag);
+            worldAccess.readAdditionalLevelSaveData(false);
         } catch (NbtException | ReportedNbtException | IOException var10) {
             this.minecraft.setScreen(new RecoverWorldDataScreen(this.minecraft, success -> {
                 if (success) {
@@ -382,11 +_,20 @@
         WorldData data = worldStem.worldData();
         boolean oldCustomized = data.worldGenOptions().isOldCustomizedWorld();
         boolean unstable = data.worldGenSettingsLifecycle() != Lifecycle.stable();
-        if (!oldCustomized && !unstable) {
+        boolean skipConfirmation = data instanceof PrimaryLevelData pld && pld.hasConfirmedExperimentalWarning();
+        if (skipConfirmation || (!oldCustomized && !unstable)) {
             this.openWorldLoadBundledResourcePack(worldAccess, worldStem, packRepository, onCancel);
         } else {
             this.askForBackup(
-                worldAccess, oldCustomized, () -> this.openWorldLoadBundledResourcePack(worldAccess, worldStem, packRepository, onCancel), () -> {
+                worldAccess, oldCustomized, () -> {
+                    if (!oldCustomized) {
+                        // Neo: Prevent the message from showing again
+                        if (worldStem.worldData() instanceof PrimaryLevelData pld) {
+                            pld.withConfirmedWarning(true);
+                        }
+                    }
+                    this.openWorldLoadBundledResourcePack(worldAccess, worldStem, packRepository, onCancel);
+                }, () -> {
                     worldStem.close();
                     worldAccess.safeClose();
                     onCancel.run();
