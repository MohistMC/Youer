--- a/net/minecraft/client/renderer/block/model/ItemModelGenerator.java
+++ b/net/minecraft/client/renderer/block/model/ItemModelGenerator.java
@@ -58,8 +_,8 @@
                 break;
             }
 
-            SpriteContents sprite = modelBaker.sprites().get(material, name).contents();
-            elements.addAll(processFrames(layerIndex, textureReference, sprite));
+            net.minecraft.client.renderer.texture.TextureAtlasSprite sprite = modelBaker.sprites().get(material, name);
+            elements.addAll(processFrames(layerIndex, textureReference, sprite.contents()));
         }
 
         return SimpleUnbakedGeometry.bake(elements, textureSlots, modelBaker, modelState, name);
