--- a/net/minecraft/server/PlayerAdvancements.java
+++ b/net/minecraft/server/PlayerAdvancements.java
@@ -1,5 +_,6 @@
 package net.minecraft.server;
 
+import com.google.common.util.concurrent.ThreadFactoryBuilder;
 import com.google.gson.Gson;
 import com.google.gson.GsonBuilder;
 import com.google.gson.JsonElement;
@@ -8,9 +_,14 @@
 import com.google.gson.internal.Streams;
 import com.google.gson.stream.JsonReader;
 import com.mojang.datafixers.DataFixer;
+import com.mojang.datafixers.util.Pair;
 import com.mojang.logging.LogUtils;
 import com.mojang.serialization.Codec;
+import com.mojang.serialization.DataResult;
+import com.mojang.serialization.DynamicOps;
 import com.mojang.serialization.JsonOps;
+import io.papermc.paper.util.concurrent.ScalingThreadPool;
+import it.unimi.dsi.fastutil.objects.Object2ObjectOpenHashMap;
 import java.io.IOException;
 import java.io.Writer;
 import java.nio.charset.StandardCharsets;
@@ -22,6 +_,10 @@
 import java.util.Map;
 import java.util.Set;
 import java.util.Map.Entry;
+import java.util.concurrent.CompletableFuture;
+import java.util.concurrent.ExecutionException;
+import java.util.concurrent.ThreadPoolExecutor;
+import java.util.concurrent.TimeUnit;
 import java.util.function.BiConsumer;
 import javax.annotation.Nullable;
 import net.minecraft.FileUtil;
@@ -44,13 +_,30 @@
 import net.minecraft.server.players.PlayerList;
 import net.minecraft.util.datafix.DataFixTypes;
 import net.minecraft.world.level.GameRules;
+import org.bukkit.Bukkit;
+import org.bukkit.event.player.PlayerAdvancementDoneEvent;
 import org.slf4j.Logger;
 
 public class PlayerAdvancements {
     private static final Logger LOGGER = LogUtils.getLogger();
-    private static final Gson GSON = new GsonBuilder().setPrettyPrinting().create();
+    private static final Gson GSON = (new GsonBuilder()).create(); // Paper - Remove pretty printing from advancements
+    // Youer start - Async advancement loading
+    // Dedicated scaling thread pool: parses the (potentially huge) advancement files off the main thread,
+    // so multiple players logging in at once don't stall the server thread. The queue only accepts work
+    // when a worker is idle, so the pool can grow up to the max while stays at core size otherwise.
+    private static final ThreadPoolExecutor LOAD_EXECUTOR = new ThreadPoolExecutor(
+        2,
+        Math.min(8, Runtime.getRuntime().availableProcessors()),
+        60L,
+        TimeUnit.SECONDS,
+        ScalingThreadPool.createUnboundedQueue(),
+        new ThreadFactoryBuilder().setNameFormat("Youer Advancement Loader - %d").build(),
+        ScalingThreadPool.defaultReEnqueuePolicy()
+    );
+    // Youer end
     private final PlayerList playerList;
     private final Path playerSavePath;
+    private final ServerAdvancementManager advancementManager; // Youer - keep the manager for the async apply
     private AdvancementTree tree;
     private final Map<AdvancementHolder, AdvancementProgress> progress = new LinkedHashMap<>();
     private final Set<AdvancementHolder> visible = new HashSet<>();
@@ -61,15 +_,27 @@
     private AdvancementHolder lastSelectedTab;
     private boolean isFirstPacket = true;
     private final Codec<PlayerAdvancements.Data> codec;
+    private final CompletableFuture<PlayerAdvancements.Data> pendingLoad; // Youer - async parse of the file
+    private boolean loaded; // Youer - guard against applying the parsed data twice
 
     public PlayerAdvancements(DataFixer p_265655_, PlayerList p_265703_, ServerAdvancementManager p_265166_, Path p_265268_, ServerPlayer p_265673_) {
         this.playerList = p_265703_;
         this.playerSavePath = p_265268_;
         this.player = p_265673_;
         this.tree = p_265166_.tree();
+        this.advancementManager = p_265166_;
         int i = 1343;
         this.codec = DataFixTypes.ADVANCEMENTS.wrapCodec(PlayerAdvancements.Data.CODEC, p_265655_, 1343);
-        this.load(p_265166_);
+        // Youer start - Async advancement loading: parse the file off the main thread, apply at the sync point (placeNewPlayer)
+        if (com.mohistmc.youer.YouerConfig.async_advancement_loading) {
+            this.pendingLoad = CompletableFuture.supplyAsync(this::parseFile, LOAD_EXECUTOR);
+        } else {
+            // Sync fallback: keep the vanilla behaviour when the option is disabled
+            this.pendingLoad = CompletableFuture.completedFuture(null);
+            this.loaded = true;
+            this.load(this.advancementManager);
+        }
+        // Youer end
     }
 
     public void setPlayer(ServerPlayer p_135980_) {
@@ -83,6 +_,7 @@
     }
 
     public void reload(ServerAdvancementManager p_135982_) {
+        this.ensureLoaded(); // Youer - wait for the pending async parse before resetting the state
         this.stopListening();
         this.progress.clear();
         this.visible.clear();
@@ -91,6 +_,7 @@
         this.isFirstPacket = true;
         this.lastSelectedTab = null;
         this.tree = p_135982_.tree();
+        // Youer - reload is rare (datapack reload), so it re-parses synchronously
         this.load(p_135982_);
     }
 
@@ -110,13 +_,20 @@
         }
     }
 
+    // Youer start - Async advancement loading
+    // Synchronous load, kept for reload and for FakePlayerAdvancements which overrides it to do nothing.
     protected void load(ServerAdvancementManager p_136007_) {
+        this.applyLoadedData(p_136007_, this.parseFile());
+    }
+
+    // Runs on a worker thread: pure file + codec parsing, no shared state, no events.
+    // Returns null (and logs) when the file is missing or cannot be parsed, like the old sync load did.
+    private PlayerAdvancements.Data parseFile() {
         if (Files.isRegularFile(this.playerSavePath)) {
             try (JsonReader jsonreader = new JsonReader(Files.newBufferedReader(this.playerSavePath, StandardCharsets.UTF_8))) {
                 jsonreader.setLenient(false);
                 JsonElement jsonelement = Streams.parse(jsonreader);
-                PlayerAdvancements.Data playeradvancements$data = this.codec.parse(JsonOps.INSTANCE, jsonelement).getOrThrow(JsonParseException::new);
-                this.applyFrom(p_136007_, playeradvancements$data);
+                return this.codec.parse(JsonOps.INSTANCE, jsonelement).getOrThrow(JsonParseException::new);
             } catch (JsonIOException | IOException ioexception) {
                 LOGGER.error("Couldn't access player advancements in {}", this.playerSavePath, ioexception);
             } catch (JsonParseException jsonparseexception) {
@@ -124,11 +_,46 @@
             }
         }
 
+        return null;
+    }
+
+    // Applies the parsed data on the calling thread. Must only run on the main thread
+    // (it fires events, grants rewards and registers trigger listeners).
+    private void applyLoadedData(ServerAdvancementManager p_136007_, @Nullable PlayerAdvancements.Data p_301198_) {
+        if (p_301198_ != null) {
+            this.applyFrom(p_136007_, p_301198_);
+        }
         this.checkForAutomaticTriggers(p_136007_);
         this.registerListeners(p_136007_);
     }
 
+    /**
+     * Waits for the asynchronous advancement parse and applies the data on the calling thread.
+     * The parse itself runs on a worker thread and never depends on the main thread, so this
+     * cannot deadlock; under normal login flow the parse finishes while the client is still in
+     * the configuration phase and this returns immediately.
+     */
+    public void ensureLoaded() {
+        if (this.loaded) {
+            return;
+        }
+        this.loaded = true;
+        PlayerAdvancements.Data data = null;
+        try {
+            data = this.pendingLoad.get();
+        } catch (InterruptedException e) {
+            Thread.currentThread().interrupt();
+            LOGGER.error("Interrupted while loading player advancements in {}", this.playerSavePath, e);
+        } catch (ExecutionException e) {
+            LOGGER.error("Failed to load player advancements in {}", this.playerSavePath, e);
+        }
+        this.applyLoadedData(this.advancementManager, data);
+    }
+    // Youer end
+
     public void save() {
+        if (org.spigotmc.SpigotConfig.disableAdvancementSaving) return; // Spigot
+        this.ensureLoaded(); // Youer - never overwrite the file with data that hasn't been loaded yet
         JsonElement jsonelement = this.codec.encodeStart(JsonOps.INSTANCE, this.asData()).getOrThrow();
 
         try {
@@ -146,6 +_,8 @@
         p_301198_.forEach((p_300732_, p_300733_) -> {
             AdvancementHolder advancementholder = p_301166_.get(p_300732_);
             if (advancementholder == null) {
+                if (!p_300732_.getNamespace().equals("minecraft")) return; // CraftBukkit
+                if (!org.purpurmc.purpur.PurpurConfig.loggerSuppressIgnoredAdvancementWarnings) // Purpur
                 LOGGER.warn("Ignored advancement '{}' in progress file {} - it doesn't exist anymore?", p_300732_, this.playerSavePath);
             } else {
                 this.startProgress(advancementholder, p_300733_);
@@ -170,15 +_,34 @@
         AdvancementProgress advancementprogress = this.getOrStartProgress(p_300979_);
         boolean flag1 = advancementprogress.isDone();
         if (advancementprogress.grantProgress(p_135990_)) {
+            // Paper start - Add PlayerAdvancementCriterionGrantEvent
+            if (!new com.destroystokyo.paper.event.player.PlayerAdvancementCriterionGrantEvent(this.player.getBukkitEntity(), p_300979_.toBukkit(), p_135990_).callEvent()) {
+                advancementprogress.revokeProgress(p_135990_);
+                return false;
+            }
+            // Paper end - Add PlayerAdvancementCriterionGrantEvent
             this.unregisterListeners(p_300979_);
             this.progressChanged.add(p_300979_);
             flag = true;
+            net.neoforged.neoforge.event.EventHooks.onAdvancementProgressedEvent(this.player, p_300979_, advancementprogress, p_135990_, net.neoforged.neoforge.event.entity.player.AdvancementEvent.AdvancementProgressEvent.ProgressType.GRANT);
             if (!flag1 && advancementprogress.isDone()) {
+                // Paper start - Add Adventure message to PlayerAdvancementDoneEvent
+                final net.kyori.adventure.text.Component message = p_300979_.value().display().flatMap(info -> {
+                    return java.util.Optional.ofNullable(
+                            info.shouldAnnounceChat() ? io.papermc.paper.adventure.PaperAdventure.asAdventure(info.getType().createAnnouncement(p_300979_, this.player)) : null
+                    );
+                }).orElse(null);
+                final org.bukkit.event.player.PlayerAdvancementDoneEvent event = new org.bukkit.event.player.PlayerAdvancementDoneEvent(this.player.getBukkitEntity(), p_300979_.toBukkit(), message);
+                this.player.level().getCraftServer().getPluginManager().callEvent(event); // CraftBukkit
+                // Paper end
                 p_300979_.value().rewards().grant(this.player);
                 p_300979_.value().display().ifPresent(p_352686_ -> {
-                    if (p_352686_.shouldAnnounceChat() && this.player.level().getGameRules().getBoolean(GameRules.RULE_ANNOUNCE_ADVANCEMENTS)) {
-                        this.playerList.broadcastSystemMessage(p_352686_.getType().createAnnouncement(p_300979_, this.player), false);
+                    // Paper start - Add Adventure message to PlayerAdvancementDoneEvent
+                    if (event.message() != null && this.player.level().getGameRules().getBoolean(GameRules.RULE_ANNOUNCE_ADVANCEMENTS)) {
+                        this.playerList.broadcastSystemMessage(io.papermc.paper.adventure.PaperAdventure.asVanilla(event.message()), false);
+                        // Paper end
                     }
+                    net.neoforged.neoforge.event.EventHooks.onAdvancementEarnedEvent(this.player, p_300979_);
                 });
             }
         }
@@ -198,6 +_,7 @@
             this.registerListeners(p_301073_);
             this.progressChanged.add(p_301073_);
             flag = true;
+            net.neoforged.neoforge.event.EventHooks.onAdvancementProgressedEvent(this.player, p_301073_, advancementprogress, p_136000_, net.neoforged.neoforge.event.entity.player.AdvancementEvent.AdvancementProgressEvent.ProgressType.REVOKE);
         }
 
         if (flag1 && !advancementprogress.isDone()) {
@@ -318,8 +_,45 @@
         );
     }
 
+    // Youer start - Fast advancement map codec
+    // The vanilla unboundedMap codec collects into an Object2ObjectArrayMap whose putIfAbsent does a
+    // linear scan, i.e. O(n^2) total. For large advancement files (big modpacks can easily have tens of
+    // thousands of entries) that dominated login time. Decode into an open hash map with a pre-allocated
+    // capacity instead; encoding keeps using the vanilla codec (no performance requirement there).
+    private static final Codec<Map<ResourceLocation, AdvancementProgress>> UNBOUNDED_MAP_CODEC = Codec.unboundedMap(ResourceLocation.CODEC, AdvancementProgress.CODEC);
+    private static final Codec<Map<ResourceLocation, AdvancementProgress>> FAST_MAP_CODEC = new Codec<>() {
+        @Override
+        public <T> DataResult<Pair<Map<ResourceLocation, AdvancementProgress>, T>> decode(DynamicOps<T> ops, T input) {
+            return ops.getMapValues(input).flatMap(stream -> {
+                java.util.List<Pair<T, T>> list = stream.collect(java.util.stream.Collectors.toList());
+                Object2ObjectOpenHashMap<ResourceLocation, AdvancementProgress> map = new Object2ObjectOpenHashMap<>(Math.max(16, list.size() + 1));
+                for (Pair<T, T> entry : list) {
+                    DataResult<ResourceLocation> keyResult = ResourceLocation.CODEC.parse(ops, entry.getFirst());
+                    if (keyResult.error().isPresent()) {
+                        return DataResult.error(() -> "Failed to parse advancement id: " + keyResult.error().get().message());
+                    }
+                    DataResult<AdvancementProgress> valueResult = AdvancementProgress.CODEC.parse(ops, entry.getSecond());
+                    if (valueResult.error().isPresent()) {
+                        return DataResult.error(() -> "Failed to parse advancement progress for '" + keyResult.getOrThrow() + "': " + valueResult.error().get().message());
+                    }
+                    ResourceLocation key = keyResult.getOrThrow();
+                    if (map.putIfAbsent(key, valueResult.getOrThrow()) != null) {
+                        return DataResult.error(() -> "Duplicate advancement id: " + key);
+                    }
+                }
+                return DataResult.success(Pair.of(map, ops.empty()));
+            });
+        }
+
+        @Override
+        public <T> DataResult<T> encode(Map<ResourceLocation, AdvancementProgress> map, DynamicOps<T> ops, T prefix) {
+            return UNBOUNDED_MAP_CODEC.encode(map, ops, prefix);
+        }
+    };
+    // Youer end
+
     static record Data(Map<ResourceLocation, AdvancementProgress> map) {
-        public static final Codec<PlayerAdvancements.Data> CODEC = Codec.unboundedMap(ResourceLocation.CODEC, AdvancementProgress.CODEC)
+        public static final Codec<PlayerAdvancements.Data> CODEC = FAST_MAP_CODEC
             .xmap(PlayerAdvancements.Data::new, PlayerAdvancements.Data::map);
 
         public void forEach(BiConsumer<ResourceLocation, AdvancementProgress> p_300973_) {
