# 2026-09-06

## 清除 patches 下的"删除类"补丁
- 需求：删除 `patches/` 下所有**第二行为 `+++ /dev/null`** 的补丁文件（这类补丁是 NeoForge 对 MC 原版类的整文件删除）。
- 执行：Python 遍历 1774 个 `.patch`，按 `第二行 == "+++ /dev/null"` 精确匹配（兼容 CRLF），命中 **184** 个；删除前二次校验行内容再 `os.remove`。
- 结果：patches 文件数 1774 → 1590，剩余命中 0。git 工作区此前干净，184 个文件全部受版本控制，`git checkout -- patches/` 可完整恢复。
- 备份：`C:\Users\Mgazul\AppData\Local\Temp\devnull_patches_backup.zip`（184 个文件，184 KB）。
- 残留 8 个空目录（git 不跟踪空目录，可忽略）：
  `core/registries/codec`、`levelgen/blockpredicates`、`levelgen/densityfunction/generator`、`levelgen/densityfunction/op`、`levelgen/material/condition`、`levelgen/material/rule`、`loot/providers/number/floats`、`loot/providers/number/ints`
- 惯例：批量删除前先 zip 备份到 TEMP + 二次校验行内容，再执行删除。

## 环境/工具经验
- 用 `sed` 逐个文件读第二行在 1774 文件规模下会超过 120s 前台超时，改用 Python 一次遍历（只 read 前 4096 字节）秒级完成。
- Bash 沙箱下批量 `os.remove` 会被中断（本次删到 49 个后报 FileNotFoundError），脚本需写成容错（跳过已不存在的文件）后重跑即可补齐。
